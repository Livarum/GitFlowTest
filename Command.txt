
///////////////////////////////////////
Install CLI 
    npm install -g @angular/cli

New project
    ng new your-project-name

Go to project
    cd your-project-name

Generate new component
    ng generate component example

Generate new service
    ng generate service services/auth

Generate new module
ng generate module dashboard

Generate new module-routing
ng generate module dashboard/dashboard-routing --flat





///////////////////////////////////////
Create controller
    php artisan make:controller AuthController

Run specific seeder
    php artisan db:seed --class=ProductSeeder

Create model with all resources
    php artisan make:model Product --all

php artisan optimize:clear

php artisan config:clear

php artisan migrate

Make seeder
    php artisan make:seeder RolesSeeder

Rollback seeder
    php artisan migrate:refresh --seed

Rollback specific seeder
    php artisan db:seed --class=RolesSeeder


///////////////////////////////////////
git init
git add .
git commit -m "Commit 0"
git remote add origin https://github.com/Livarum/GitFlowTest.git
git push -u origin master
git config --global user.email "alexpersonal0815@gmail.com"
git config --global user.name "alexram"

